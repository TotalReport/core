import LaunchesContainer from "@/containers/launches/launches-container";

export const metadata = {
  title: "Launches - Total Report",
};

export default function LaunchesPage() {
  return <LaunchesContainer />;
}
