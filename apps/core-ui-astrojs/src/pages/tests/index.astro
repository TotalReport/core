---
import Layout from "../../layouts/Layout.astro";
import { Layout as PageLayout } from "../../components/Layout.tsx";
import { RestAPIProvider } from "@/components/RestAPIProvider";
import { TestsList } from "@/components/Tests";
---

<Layout>
  <RestAPIProvider client:only>
    <PageLayout client:only >
      <TestsList client:only></TestsList>
    </PageLayout>
  </RestAPIProvider>
</Layout>
